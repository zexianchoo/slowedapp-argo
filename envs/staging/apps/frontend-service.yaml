apiVersion: v1
kind: Service
metadata:
  name: slowedapp-frontend-service
  namespace: slowedapp
  labels:
    app: slowedapp-frontend-dev 
spec:
  type: NodePort
  selector:
    app: slowedapp-frontend-dev  
  ports:
    - protocol: TCP
      port: 80 
      targetPort: 5173 
      nodePort: 30000 